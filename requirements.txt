flask==3.0.3
line-bot-sdk==3.5.0  # 升級到最新版本，支援 Python 3.13 和較新的 aiohttp
gspread==6.1.2
google-auth==2.35.0
google-generativeai==0.8.3
python-dotenv==1.0.1
gunicorn==23.0.0
pydantic==2.0.3  # 與 line-bot-sdk 3.5.0 兼容
requests==2.31.0  # 與 line-bot-sdk 3.5.0 兼容
# 不明確指定 aiohttp 版本，讓 line-bot-sdk 自動處理依賴
