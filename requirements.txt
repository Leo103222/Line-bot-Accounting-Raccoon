flask==3.0.3
line-bot-sdk==2.3.0  # 回退到穩定版本，支援較新 aiohttp
gspread==6.1.2
google-auth==2.35.0
google-generativeai==0.8.3
python-dotenv==1.0.1
gunicorn==23.0.0
pydantic==1.10.13  # 與 line-bot-sdk 2.3.0 兼容
requests==2.28.2   # 與 line-bot-sdk 2.3.0 兼容
aiohttp==3.10.5    # 明確指定支持 Python 3.13 的版本
cryptography==43.0.3  # 明確指定最新版本，避免 Rust 編譯問題